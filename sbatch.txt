#!/bin/bash
# Created by the University of Melbourne job script generator for SLURM
# Mon Sep 29 2025 14:20:10 GMT+1000 (Australian Eastern Standard Time)

# Partition for the job:
#SBATCH --partition=sapphire

# Multithreaded (SMP) job: must run on one node 
#SBATCH --nodes=1

# The name of the job:
#SBATCH --job-name="metagenomic pipeline head"

# The project ID which this job should run under:
#SBATCH --account="punim2582"

# Maximum number of tasks/CPU cores used by the job:
#SBATCH --ntasks=1
#SBATCH --cpus-per-task=1

# Use this email address:
#SBATCH --mail-user=z.khurram@student.unimelb.edu.au

# Send yourself an email when the job:
# aborts abnormally (fails)
#SBATCH --mail-type=FAIL
# begins
#SBATCH --mail-type=BEGIN
# ends successfully
#SBATCH --mail-type=END

# The maximum running time of the job in days-hours:mins:sec
#SBATCH --time=2-0:0:00

# check that the script is launched with sbatch
if [ "x$SLURM_JOB_ID" == "x" ]; then
   echo "You need to submit your job to the queuing system with sbatch"
   exit 1
fi

# Run the job from the directory where it was launched (default)

# The modules to load:
module load Nextflow/24.10.2
module load GCCcore/11.3.0
module load Apptainer/1.3.3
module load Aspera-Connect/4.2.6

# The job command(s):
nextflow run main.nf --filepath /home/zkhurram/projectfolder/serine_project/raw_reads/250425_LH00444_0332_A2323YJLT3/  --resume

##DO NOT ADD/EDIT BEYOND THIS LINE##
##Job monitor command to list the resource usage
my-job-stats -a -n -s
